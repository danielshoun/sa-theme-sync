# Something Awful Theme Sync

A Chrome extension that automatically toggles the Something Awful Forums dark theme to match your system's light/dark mode preference.

## Installation

1. Open `chrome://extensions` in Chrome.
2. Enable **Developer mode**.
3. Click **Load unpacked** and select this folder.

The extension activates automatically on `forums.somethingawful.com`.
